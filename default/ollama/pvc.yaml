---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-config
  namespace: default
  annotations:
    nfs.io/storage-path: "containers/kubernetes/ollama/config"
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: "nfs-dynamic"
  resources:
    requests:
      storage: 100Gi
