apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: calibre-credentials
spec:
  refreshInterval: "1h"
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: calibre-credentials
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        GUAC_USER: "{{ .guac_user }}"
        GUAC_PASS: "{{ .guac_pass }}"
  data:
    - secretKey: "guac_user"
      remoteRef:
        metadataPolicy: Fetch
        key: "calibre-credentials"
        property: "guac-user"
    - secretKey: "guac_pass"
      remoteRef:
        metadataPolicy: Fetch
        key: "calibre-credentials"
        property: "guac-pass"
