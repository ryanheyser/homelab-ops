---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: etherpad-tailscale
spec:
  defaultBackend:
    service:
      name: etherpad
      port:
        number: 9001
  ingressClassName: tailscale
  tls:
    - hosts:
        - etherpad