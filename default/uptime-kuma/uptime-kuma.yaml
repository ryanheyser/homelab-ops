apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: uptime-kuma
  namespace: default
spec:
  driftDetection:
    mode: enabled
  interval: 15m
  chart:
    spec:
      # renovate: registryUrl=https://bjw-s-labs.github.io/helm-charts
      chart: app-template
      version: 4.6.2
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      main:
        defaultContainerOptions:
          resources:
            requests:
              cpu: 10m
              memory: 250Mi
            limits:
              memory: 2Gi
        annotations:
          reloader.stakater.com/auto: "true"
        pod:
          securityContext:
            fsGroup: 3001
            fsGroupChangePolicy: "OnRootMismatch"
            supplementalGroups: [ 568, 911, 1000, 1001 ]
          tolerations:
          - key: "arm"
            operator: "Exists"
        containers:
          main:
            image:
              # renovate: repository=ghcr.io/louislam/uptime-kuma
              repository: ghcr.io/louislam/uptime-kuma
              tag: 2.2.1
            securityContext:
              runAsUser: 1000
              runAsGroup: 3001
            env:
              TZ: "America/New_York"
              UPTIME_KUMA_PORT: &port "3001"
    service:
      main:
        controller: main
        type: ClusterIP
        ports:
          http:
            port: *port

    ingress:
      main:
        className: 'traefik'
        annotations:
          kubernetes.io/ingress.class: traefik
          traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
          traefik.ingress.kubernetes.io/router.middlewares: "network-system-authentik@kubernetescrd"
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Media
          gethomepage.dev/icon: uptime-kuma.svg
          gethomepage.dev/name: Uptime Kuma
          gethomepage.dev/description: Uptime Kuma is a self-hosted monitoring tool that allows you to monitor your services and websites.
        hosts:
        - host: &host "uptime-kuma.homelab.heyser.xyz"
          paths:
          - path: /
            pathType: Prefix
            service:
              identifier: main
              port: http
        tls:
        - hosts:
          - *host
          secretName: uptime-kuma-homelab-heyser-xyz-tls

    # route:
    #   main:
    #     enabled: true
    #     kind: HTTPRoute
    #     parentRefs:
    #     - group: gateway.networking.k8s.io
    #       kind: Gateway
    #       name: traefik-gateway
    #       namespace: network-system
    #     hostnames:
    #     - *host
    #     rules:
    #     - matches:
    #       - path:
    #           type: PathPrefix
    #           value: /
    #       backendRefs:
    #       - kind: Service
    #         name: audiobookshelf
    #         port: *port
    #       timeouts:
    #         backendRequest: 10s
    #         request: 30s

    persistence:
      config:
        existingClaim: uptime-kuma-config
        advancedMounts:
          main:
            main:
            - path: "/app/data"
